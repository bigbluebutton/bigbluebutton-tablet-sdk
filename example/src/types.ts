export type INativeEvent = {
  canGoBack: Boolean;
  canGoForward: Boolean;
  code: Number;
  description: String;
  didFailProvisionalNavigation: Boolean;
  domain: String;
  loading: Boolean;
  target: Number;
  title: String;
  url: String;
};
